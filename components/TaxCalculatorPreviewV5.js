'use client';

import { useMemo, useState } from 'react';
import {
  PTKP,
  calculateAnnualPph21ByMethod,
  calculateMonthlyPph21ByMethod,
  calculateUmkmFinal,
  calculateVat,
  formatRupiah
} from '../lib/tax';
import { PPH21_CODE_GROUPS, PPH21_CODES } from '../lib/pph21Codes';

const tabs = [
  ['pph21', 'PPh 21'],
  ['ppn', 'PPN'],
  ['umkm', 'PPh Final UMKM'],
  ['unifikasi', 'PPh Unifikasi'],
  ['badan', 'PPh Badan']
];

const cards = {
  pph21: {
    kicker: 'Peraturan terbaru · PPh 21',
    title: 'PP 58/2023, PMK 168/2023 & PER-11/PJ/2025',
    text: 'TER berlaku untuk pegawai tetap pada masa selain masa pajak terakhir. Kode objek bukti potong mengikuti PER-11/PJ/2025.',
    links: [
      ['PP 58 Tahun 2023', 'https://jdih.kemenkeu.go.id/dok/pp-58-tahun-2023'],
      ['PMK 168 Tahun 2023', 'https://jdih.kemenkeu.go.id/dok/pmk-168-tahun-2023'],
      ['PER-11/PJ/2025', 'https://www.pajak.go.id/id/peraturan/ketentuan-pelaporan-pajak-penghasilan-pajak-pertambahan-nilai-pajak-penjualan-atas-0']
    ]
  },
  ppn: {
    kicker: 'Peraturan terbaru · PPN',
    title: 'PMK 131/2024',
    text: 'Transaksi umum non-mewah memakai tarif formal 12% dengan DPP Nilai Lain 11/12 sehingga beban efektif tetap 11%.',
    links: [['PMK 131 Tahun 2024', 'https://jdih.kemenkeu.go.id/dok/pmk-131-tahun-2024']]
  },
  umkm: {
    kicker: 'Peraturan terbaru · PPh Final UMKM',
    title: 'PP 20/2026',
    text: 'Skema PPh Final UMKM 0,5% diperbarui, termasuk subjek, batas waktu, agregasi omzet, dan ketentuan transisi.',
    links: [['PP 20 Tahun 2026', 'https://jdih.kemenkeu.go.id/dok/pp-20-tahun-2026']]
  },
  unifikasi: {
    kicker: 'Peraturan terbaru · PPh Unifikasi',
    title: 'Coretax & PMK 81/2024',
    text: 'Administrasi melalui Coretax; tarif mengikuti objek PPh 4(2), 15, 22, 23, atau 26.',
    links: [
      ['PMK 81 Tahun 2024', 'https://jdih.kemenkeu.go.id/dok/pmk-81-tahun-2024'],
      ['PMK 1 Tahun 2026', 'https://jdih.kemenkeu.go.id/dok/pmk-1-tahun-2026']
    ]
  },
  badan: {
    kicker: 'Peraturan terbaru · PPh Badan',
    title: '22%, 19%, Pasal 31E & Final UMKM 0,5%',
    text: 'PPh Badan tidak hanya 22%. Kalkulator mendukung tarif umum 22%, Perseroan Terbuka 19%, fasilitas Pasal 31E (efektif 11% pada bagian yang mendapat fasilitas), serta PPh Final UMKM 0,5% untuk PT Perorangan/Koperasi yang memenuhi syarat PP 20/2026.',
    links: [
      ['UU 7 Tahun 2021 (HPP)', 'https://jdih.kemenkeu.go.id/dok/uu-7-tahun-2021/overview'],
      ['PP 55 Tahun 2022', 'https://jdih.kemenkeu.go.id/dok/pp-55-tahun-2022'],
      ['PP 20 Tahun 2026', 'https://jdih.kemenkeu.go.id/dok/pp-20-tahun-2026'],
      ['PMK 72 Tahun 2023', 'https://jdih.kemenkeu.go.id/dok/pmk-72-tahun-2023'],
      ['Mekanisme PPh Badan DJP', 'https://www.pajak.go.id/id/mekanisme-penghitungan-pajak-penghasilan-badan']
    ]
  }
};

const uni = [
  ['p23j', 'PPh 23 — Jasa teknik/manajemen/konsultan/jasa lain', 'PPh Pasal 23', 2],
  ['p23s', 'PPh 23 — Sewa harta selain tanah & bangunan', 'PPh Pasal 23', 2],
  ['p23b', 'PPh 23 — Bunga, royalti, hadiah tertentu', 'PPh Pasal 23', 15],
  ['sTB', 'PPh Final 4(2) — Sewa tanah/bangunan', 'PPh Pasal 4 ayat (2)', 10],
  ['k1', 'PPh Final 4(2) — Konstruksi kecil tersertifikasi', 'PPh Pasal 4 ayat (2)', 1.75],
  ['k2', 'PPh Final 4(2) — Konstruksi tersertifikasi selain kecil', 'PPh Pasal 4 ayat (2)', 2.65],
  ['k3', 'PPh Final 4(2) — Konstruksi tanpa sertifikat', 'PPh Pasal 4 ayat (2)', 4],
  ['p15a', 'PPh 15 — Charter penerbangan dalam negeri', 'PPh Pasal 15', 1.8],
  ['p15p', 'PPh 15 — Pelayaran dalam negeri', 'PPh Pasal 15', 1.2],
  ['p22a', 'PPh 22 — Impor API', 'PPh Pasal 22', 2.5],
  ['p22n', 'PPh 22 — Impor non-API', 'PPh Pasal 22', 7.5],
  ['p22b', 'PPh 22 — Pembelian barang bendahara/BUMN/BUMD', 'PPh Pasal 22', 1.5],
  ['p22s', 'PPh 22 — Sistem Informasi Pengadaan', 'PPh Pasal 22', 0.5],
  ['p22e', 'PPh 22 — Emas perhiasan/batangan', 'PPh Pasal 22', 0.25],
  ['p26', 'PPh 26 — WPLN tarif domestik', 'PPh Pasal 26', 20],
  ['custom', 'Tarif khusus/treaty/objek lain', 'PPh Unifikasi — tarif khusus', null]
];

function Money({ label, value, setValue, hint }) {
  return <label className="field"><span>{label}</span><div className="money-input"><span>Rp</span><input type="number" min="0" value={value} onChange={(e) => setValue(e.target.value)} /></div>{hint && <small>{hint}</small>}</label>;
}
function Row({ label, value, strong = false, note }) {
  return <div className={`result-line ${strong ? 'strong' : ''}`}><div><span>{label}</span>{note && <small>{note}</small>}</div><b>{value}</b></div>;
}

function P21() {
  const [mode, setMode] = useState('monthly'), [method, setMethod] = useState('gross'), [gm, setGm] = useState('10000000'), [status, setStatus] = useState('K/0'), [code, setCode] = useState('21-100-01'), [ag, setAg] = useState('120000000'), [pen, setPen] = useState('1200000'), [months, setMonths] = useState('12'), [prior, setPrior] = useState('2200000');
  const monthly = useMemo(() => calculateMonthlyPph21ByMethod({ amount: gm, ptkpStatus: status, method }), [gm, status, method]);
  const annual = useMemo(() => calculateAnnualPph21ByMethod({ annualGross: ag, pensionContribution: pen, ptkpStatus: status, monthsWorked: months, priorWithheld: prior, method }), [ag, pen, status, months, prior, method]);
  const annualMode = mode !== 'monthly', object = PPH21_CODES.find((item) => item.code === code), compatible = code === '21-100-01';
  return <div className="calculator-grid"><section className="panel form-panel"><div className="method-segmented" style={{ gridTemplateColumns: 'repeat(2,1fr)' }}>{[['gross', 'Gross'], ['grossUp', 'Gross Up']].map(([value, label]) => <button key={value} className={method === value ? 'active' : ''} onClick={() => setMethod(value)}>{label}</button>)}</div><div className="segmented pph21-mode">{[['monthly', 'Masa selain terakhir'], ['annual', 'Masa pajak terakhir'], ['a1', 'BP A1']].map(([value, label]) => <button key={value} className={mode === value ? 'active' : ''} onClick={() => setMode(value)}>{label}</button>)}</div><label className="field"><span>Status PTKP</span><select value={status} onChange={(e) => setStatus(e.target.value)}>{Object.entries(PTKP).map(([key, value]) => <option key={key} value={key}>{key} - {formatRupiah(value)}</option>)}</select><small>Batas PTKP sesuai status keluarga pada awal tahun pajak.</small></label><label className="field"><span>Kode objek PPh 21</span><select value={code} onChange={(e) => setCode(e.target.value)}>{PPH21_CODE_GROUPS.map((group) => <optgroup key={group.label} label={group.label}>{group.options.map(([itemCode, label]) => <option key={itemCode} value={itemCode}>{itemCode} — {label}</option>)}</optgroup>)}</select><small>Daftar lengkap mengikuti Lampiran PER-11/PJ/2025.</small></label>{!compatible && <div className="warning-box">Kode ini tersedia untuk administrasi, tetapi formula otomatis modul ini masih difokuskan pada pegawai tetap 21-100-01.</div>}{!annualMode ? <Money label={method === 'grossUp' ? 'Penghasilan sebelum tunjangan pajak bulan ini' : 'Penghasilan bruto bulan ini'} value={gm} setValue={setGm} /> : <>{mode === 'a1' && <div className="info-box"><b>BP A1 / BPA1</b><br />Rekonsiliasi tahunan untuk membantu pengecekan bukti potong pegawai tetap/pensiunan.</div>}<Money label="Penghasilan setahun / bagian tahun" value={ag} setValue={setAg} /><Money label="Iuran pensiun/JHT" value={pen} setValue={setPen} /><label className="field"><span>Jumlah bulan bekerja</span><input type="number" min="1" max="12" value={months} onChange={(e) => setMonths(e.target.value)} /></label><Money label="PPh 21 sudah dipotong" value={prior} setValue={setPrior} /></>}</section><section className="panel result-panel"><div className="eyebrow">{mode === 'a1' ? 'Rekonsiliasi BP A1' : 'Hasil PPh 21'}</div>{!annualMode ? <><div className="hero-result"><span>PPh 21 masa ini</span><strong>{formatRupiah(monthly.tax)}</strong></div><Row label="Kode objek" value={`${code} — ${object?.label || ''}`} /><Row label="Status PTKP" value={`${status} — ${formatRupiah(PTKP[status])}`} /><Row label="Kategori TER" value={`Kategori ${monthly.category}`} /><Row label="Tarif efektif" value={`${monthly.rate}%`} /><Row label="Nilai dasar" value={formatRupiah(monthly.baseGross)} />{method === 'grossUp' && <Row label="Tunjangan pajak" value={formatRupiah(monthly.taxAllowance)} />}<Row label="Bruto untuk pajak" value={formatRupiah(monthly.gross)} /><Row label="Take-home sebelum potongan lain" value={formatRupiah(monthly.takeHome)} strong /></> : <><div className="hero-result"><span>{annual.lastPeriodTax >= 0 ? 'PPh 21 masa pajak terakhir' : 'Kelebihan pemotongan'}</span><strong>{formatRupiah(Math.abs(annual.lastPeriodTax))}</strong></div><Row label="Kode objek" value={`${code} — ${object?.label || ''}`} /><Row label="Status PTKP" value={`${status} — ${formatRupiah(PTKP[status])}`} />{mode === 'a1' && <Row label="1. Penghasilan bruto A1" value={formatRupiah(annual.gross)} strong />}<Row label={mode === 'a1' ? '2. Biaya jabatan' : 'Biaya jabatan'} value={formatRupiah(annual.positionExpense)} /><Row label={mode === 'a1' ? '3. Iuran pensiun/JHT' : 'Iuran pensiun/JHT'} value={formatRupiah(annual.pension)} /><Row label={mode === 'a1' ? '4. Penghasilan neto' : 'Penghasilan neto'} value={formatRupiah(annual.net)} /><Row label={mode === 'a1' ? '5. PTKP' : 'PTKP'} value={formatRupiah(annual.ptkp)} /><Row label={mode === 'a1' ? '6. PKP' : 'PKP'} value={formatRupiah(annual.pkp)} /><Row label={mode === 'a1' ? '7. PPh 21 setahun' : 'PPh 21 setahun'} value={formatRupiah(annual.annualTax)} strong /><Row label={mode === 'a1' ? '8. PPh 21 telah dipotong' : 'Sudah dipotong'} value={formatRupiah(annual.priorWithheld)} />{mode === 'a1' && <Row label="9. Selisih masa terakhir" value={formatRupiah(annual.lastPeriodTax)} strong />}</>}</section></div>;
}

function PPN() { const [kind, setKind] = useState('nonLuxury'), [inclusive, setInclusive] = useState(false), [amount, setAmount] = useState('100000000'); const result = useMemo(() => calculateVat({ amount, kind, inclusive }), [amount, kind, inclusive]); const formula = kind === 'nonLuxury' ? `DPP Nilai Lain = 11/12 × ${formatRupiah(result.preVat)} = ${formatRupiah(result.dpp)} · PPN = 12% × ${formatRupiah(result.dpp)} = ${formatRupiah(result.vat)}` : `PPN = 12% × ${formatRupiah(result.dpp)} = ${formatRupiah(result.vat)}`; return <div className="calculator-grid"><section className="panel form-panel"><label className="field"><span>Jenis transaksi</span><select value={kind} onChange={(e) => setKind(e.target.value)}><option value="nonLuxury">BKP non-mewah / JKP umum</option><option value="luxury">BKP mewah tertentu</option></select></label><label className="check-row"><input type="checkbox" checked={inclusive} onChange={(e) => setInclusive(e.target.checked)} /><span>Nilai sudah termasuk PPN</span></label><Money label={inclusive ? 'Nilai termasuk PPN' : 'Harga jual / penggantian sebelum PPN'} value={amount} setValue={setAmount} /></section><section className="panel result-panel"><div className="eyebrow">Hasil PPN</div><div className="hero-result"><span>PPN terutang</span><strong>{formatRupiah(result.vat)}</strong></div><Row label="Tarif formal" value="12%" /><Row label="Tarif efektif" value={`${Math.round(result.effectiveRate * 100)}%`} /><Row label="Harga sebelum PPN" value={formatRupiah(result.preVat)} /><Row label="DPP penghitungan" value={formatRupiah(result.dpp)} /><Row label="Total setelah PPN" value={formatRupiah(result.total)} strong /><div className="formula">{formula}</div></section></div>; }

function UMKM() { const [type, setType] = useState('individual'), [before, setBefore] = useState('450000000'), [month, setMonth] = useState('100000000'), [previousYear, setPreviousYear] = useState('1000000000'); const result = useMemo(() => calculateUmkmFinal({ taxpayerType: type, priorYtd: before, currentMonth: month }), [type, before, month]); const eligible = (Number(previousYear) || 0) <= 4800000000; return <div className="calculator-grid"><section className="panel form-panel"><label className="field"><span>Jenis Wajib Pajak</span><select value={type} onChange={(e) => setType(e.target.value)}><option value="individual">Orang Pribadi</option><option value="soleCompany">Perseroan Perorangan</option><option value="cooperative">Koperasi</option><option value="transition">CV / Firma / PT biasa / BUMDes</option></select></label><Money label="Peredaran bruto tahun sebelumnya" value={previousYear} setValue={setPreviousYear} /><Money label="Omzet sebelum bulan ini" value={before} setValue={setBefore} /><Money label="Omzet bulan ini" value={month} setValue={setMonth} /></section><section className="panel result-panel"><div className="eyebrow">Hasil PPh Final UMKM</div><div className="hero-result"><span>PPh Final bulan ini</span><strong>{eligible ? formatRupiah(result.tax) : 'Periksa kelayakan'}</strong></div><Row label="Omzet sebelum bulan ini" value={formatRupiah(result.before)} /><Row label="Omzet bulan ini" value={formatRupiah(result.month)} /><Row label="Omzet kumulatif" value={formatRupiah(result.after)} /><Row label="Omzet dikenai 0,5%" value={formatRupiah(result.taxableTurnover)} /><Row label="Tarif" value="0,5%" strong /></section></div>; }

function UNI() { const [id, setId] = useState('p23j'), [amount, setAmount] = useState('100000000'), [customRate, setCustomRate] = useState('1.5'); const selected = uni.find((item) => item[0] === id) || uni[0]; const rate = selected[3] ?? Math.max(0, Number(customRate) || 0); const gross = Math.max(0, Number(amount) || 0); const tax = Math.round(gross * rate / 100); return <div className="calculator-grid"><section className="panel form-panel"><label className="field"><span>Jenis objek PPh Unifikasi</span><select value={id} onChange={(e) => setId(e.target.value)}>{uni.map((item) => <option key={item[0]} value={item[0]}>{item[1]}</option>)}</select></label><Money label="Jumlah bruto / dasar pemotongan" value={amount} setValue={setAmount} />{id === 'custom' && <label className="field"><span>Tarif yang berlaku (%)</span><input type="number" value={customRate} onChange={(e) => setCustomRate(e.target.value)} /></label>}</section><section className="panel result-panel"><div className="eyebrow">Hasil PPh Unifikasi</div><div className="hero-result"><span>PPh dipotong / dipungut</span><strong>{formatRupiah(tax)}</strong></div><Row label="Jenis PPh" value={selected[2]} /><Row label="Objek" value={selected[1]} /><Row label="Tarif" value={`${rate}%`} /><Row label="Dasar pemotongan" value={formatRupiah(gross)} /></section></div>; }

const depreciationRates = { group1: { label: 'Kelompok 1 — 4 tahun', sl: 0.25, db: 0.50 }, group2: { label: 'Kelompok 2 — 8 tahun', sl: 0.125, db: 0.25 }, group3: { label: 'Kelompok 3 — 16 tahun', sl: 0.0625, db: 0.125 }, group4: { label: 'Kelompok 4 — 20 tahun', sl: 0.05, db: 0.10 }, buildingPermanent: { label: 'Bangunan permanen — 20 tahun', sl: 0.05, db: null }, buildingTemporary: { label: 'Bangunan tidak permanen — 10 tahun', sl: 0.10, db: null } };
function fiscalDepreciation(asset) { const cost = Math.max(0, Number(asset.cost) || 0), month = Math.min(12, Math.max(1, Number(asset.month) || 1)), months = 13 - month, group = depreciationRates[asset.group] || depreciationRates.group1, method = group.db === null ? 'sl' : asset.method, rate = method === 'db' ? group.db : group.sl; return Math.round(cost * rate * months / 12); }

const corporateSchemeInfo = {
  auto: { label: 'Otomatis — Pasal 31E / tarif umum', short: 'Otomatis 31E / 22%', help: 'Jika omzet sampai Rp50 miliar, kalkulator menerapkan fasilitas Pasal 31E. Jika di atas Rp50 miliar, seluruh PKP menggunakan tarif umum 22%.' },
  general22: { label: 'Tarif umum 22%', short: 'Tarif umum 22%', help: 'Menggunakan tarif umum PPh Badan 22% atas seluruh PKP tanpa fasilitas Pasal 31E.' },
  listed19: { label: 'Perseroan Terbuka 19%', short: 'Perseroan Terbuka 19%', help: 'Tarif 3% lebih rendah dari tarif umum untuk Perseroan Terbuka yang memenuhi seluruh persyaratan yang berlaku.' },
  final05: { label: 'PPh Final UMKM 0,5% — PT Perorangan/Koperasi', short: 'PPh Final UMKM 0,5%', help: 'Berdasarkan PP 20/2026, skema 0,5% dapat digunakan oleh PT Perorangan atau Koperasi yang memenuhi syarat dan omzet tidak melebihi Rp4,8 miliar. PPh dihitung dari omzet, bukan PKP.' },
  manual: { label: 'Tarif khusus / manual', short: 'Tarif manual', help: 'Gunakan hanya untuk kasus khusus yang sudah diverifikasi. Tarif manual dikenakan atas PKP.' }
};

function CorpTax() {
  const [section, setSection] = useState('summary'), [scheme, setScheme] = useState('auto'), [entityType, setEntityType] = useState('regular'), [coopEligible, setCoopEligible] = useState(true), [manualRate, setManualRate] = useState('22'), [revenue, setRevenue] = useState('10000000000'), [commercialProfit, setCommercialProfit] = useState('1200000000');
  const [corrections, setCorrections] = useState([{ id: 1, name: 'Biaya yang tidak dapat dikurangkan', type: 'positive', amount: '100000000' }, { id: 2, name: 'Penghasilan dikenai PPh Final', type: 'negative', amount: '50000000' }, { id: 3, name: 'Penghasilan bukan objek pajak', type: 'negative', amount: '0' }]);
  const [assets, setAssets] = useState([{ id: 1, name: 'Laptop & komputer', kind: 'tangible', cost: '24000000', month: '1', group: 'group1', method: 'sl', commercial: '8000000' }, { id: 2, name: 'Software khusus / lisensi', kind: 'intangible', cost: '120000000', month: '1', group: 'group2', method: 'sl', commercial: '24000000' }]);
  const [losses, setLosses] = useState(['0', '0', '0', '0', '0']);
  const [credits, setCredits] = useState({ p22: '10000000', p23: '20000000', p24: '0', p25: '100000000', other: '0' });

  const assetCalc = assets.map((asset) => { const fiscal = fiscalDepreciation(asset), commercial = Math.max(0, Number(asset.commercial) || 0), diff = commercial - fiscal; return { ...asset, fiscal, commercial, positive: Math.max(0, diff), negative: Math.max(0, -diff) }; });
  const manualPositive = corrections.filter((x) => x.type === 'positive').reduce((sum, x) => sum + Math.max(0, Number(x.amount) || 0), 0), manualNegative = corrections.filter((x) => x.type === 'negative').reduce((sum, x) => sum + Math.max(0, Number(x.amount) || 0), 0), assetPositive = assetCalc.reduce((sum, x) => sum + x.positive, 0), assetNegative = assetCalc.reduce((sum, x) => sum + x.negative, 0), positive = manualPositive + assetPositive, negative = manualNegative + assetNegative, fiscalNet = (Number(commercialProfit) || 0) + positive - negative, lossAvailable = losses.reduce((sum, value) => sum + Math.max(0, Number(value) || 0), 0), lossUsed = Math.min(Math.max(0, fiscalNet), lossAvailable), pkp = Math.max(0, Math.floor((fiscalNet - lossUsed) / 1000) * 1000), rev = Math.max(0, Number(revenue) || 0);

  const final05EntityEligible = entityType === 'soleCompany' || entityType === 'cooperative';
  const final05TurnoverEligible = rev <= 4800000000;
  const final05TimeEligible = entityType !== 'cooperative' || coopEligible;
  const final05Eligible = final05EntityEligible && final05TurnoverEligible && final05TimeEligible;

  let facilityPkp = 0, normalPkp = pkp, taxDue = 0, calculationLabel = corporateSchemeInfo[scheme].short, formulaText = '';
  if (scheme === 'auto') {
    if (rev > 0 && rev <= 50000000000) { facilityPkp = rev <= 4800000000 ? pkp : Math.min(pkp, pkp * 4800000000 / rev); normalPkp = pkp - facilityPkp; }
    taxDue = Math.round(facilityPkp * 0.11 + normalPkp * 0.22);
    calculationLabel = facilityPkp > 0 ? 'Pasal 31E + tarif umum' : 'Tarif umum 22%';
    formulaText = facilityPkp > 0 ? `PPh = 11% × ${formatRupiah(facilityPkp)} + 22% × ${formatRupiah(normalPkp)} = ${formatRupiah(taxDue)}` : `PPh = 22% × ${formatRupiah(normalPkp)} = ${formatRupiah(taxDue)}`;
  } else if (scheme === 'general22') {
    taxDue = Math.round(pkp * 0.22); formulaText = `PPh = 22% × ${formatRupiah(pkp)} = ${formatRupiah(taxDue)}`;
  } else if (scheme === 'listed19') {
    taxDue = Math.round(pkp * 0.19); formulaText = `PPh = 19% × ${formatRupiah(pkp)} = ${formatRupiah(taxDue)}`;
  } else if (scheme === 'final05') {
    taxDue = final05Eligible ? Math.round(rev * 0.005) : 0; normalPkp = 0;
    formulaText = final05Eligible ? `PPh Final = 0,5% × ${formatRupiah(rev)} = ${formatRupiah(taxDue)}` : 'Skema PPh Final 0,5% belum dapat dihitung karena syarat subjek, omzet, atau jangka waktu belum terpenuhi.';
  } else {
    const rate = Math.max(0, Number(manualRate) || 0) / 100; taxDue = Math.round(pkp * rate); formulaText = `PPh = ${Number(manualRate) || 0}% × ${formatRupiah(pkp)} = ${formatRupiah(taxDue)}`;
  }

  const totalCredits = scheme === 'final05' ? 0 : Object.values(credits).reduce((sum, x) => sum + Math.max(0, Number(x) || 0), 0), balance = taxDue - totalCredits;
  const updateCorrection = (id, key, value) => setCorrections((items) => items.map((x) => x.id === id ? { ...x, [key]: value } : x));
  const updateAsset = (id, key, value) => setAssets((items) => items.map((x) => x.id === id ? { ...x, [key]: value } : x));

  return <div className="corp-wrap"><nav className="corp-tabs">{[['summary', 'Ringkasan'], ['recon', 'Rekonsiliasi Fiskal'], ['assets', 'Penyusutan & Amortisasi'], ['credits', 'Kredit Pajak']].map(([value, label]) => <button key={value} className={section === value ? 'active' : ''} onClick={() => setSection(value)}>{label}</button>)}</nav><div className="calculator-grid"><section className="panel form-panel corp-form">
    {section === 'summary' && <><div className="eyebrow">Data utama PPh Badan</div><label className="field"><span>Skema / Tarif PPh Badan</span><select value={scheme} onChange={(e) => setScheme(e.target.value)}>{Object.entries(corporateSchemeInfo).map(([value, info]) => <option key={value} value={value}>{info.label}</option>)}</select><small>{corporateSchemeInfo[scheme].help}</small></label>{scheme === 'final05' && <><label className="field"><span>Jenis Wajib Pajak Badan</span><select value={entityType} onChange={(e) => setEntityType(e.target.value)}><option value="regular">PT biasa / CV / Firma / badan lain</option><option value="soleCompany">Perseroan Perorangan</option><option value="cooperative">Koperasi</option></select></label>{entityType === 'cooperative' && <label className="check-row"><input type="checkbox" checked={coopEligible} onChange={(e) => setCoopEligible(e.target.checked)} /><span>Koperasi masih dalam jangka waktu penggunaan tarif final yang diperbolehkan</span></label>}{!final05Eligible && <div className="warning-box">PPh Final UMKM 0,5% tahun 2026 hanya untuk subjek tertentu yang memenuhi PP 20/2026, termasuk batas omzet Rp4,8 miliar. PT biasa, CV, Firma, dan badan lain pada umumnya menggunakan ketentuan umum/Pasal 31E.</div>}</>}{scheme === 'manual' && <label className="field"><span>Tarif manual (%)</span><input type="number" min="0" step="0.01" value={manualRate} onChange={(e) => setManualRate(e.target.value)} /><small>Gunakan hanya setelah dasar hukum tarif khusus diverifikasi.</small></label>}<Money label="Peredaran bruto setahun" value={revenue} setValue={setRevenue} /><Money label="Laba / (rugi) komersial sebelum pajak" value={commercialProfit} setValue={setCommercialProfit} /><div className="rate-grid"><div><span>22%</span><small>Tarif umum</small></div><div><span>19%</span><small>Perseroan Terbuka*</small></div><div><span>11%</span><small>Efektif Pasal 31E*</small></div><div><span>0,5%</span><small>Final UMKM Badan*</small></div></div><small className="rate-note">*Hanya jika seluruh persyaratan masing-masing skema terpenuhi.</small>{scheme === 'final05' && <div className="info-box">Pada PPh Final UMKM 0,5%, rekonsiliasi fiskal dan PKP tidak menjadi dasar penghitungan PPh terutang karena pajak dihitung langsung dari peredaran bruto.</div>}<div className="corp-summary-box"><b>Skema aktif: {calculationLabel}</b><span>{scheme === 'final05' ? 'Omzet → tarif final 0,5% → PPh Final.' : 'Laba komersial → koreksi fiskal → neto fiskal → kompensasi rugi → PKP → tarif PPh Badan → kredit pajak → PPh 29/lebih bayar.'}</span></div></>}
    {section === 'recon' && <><div className="eyebrow">Rekonsiliasi Fiskal</div><p className="corp-help">Masukkan koreksi permanen atau temporer. Koreksi dari penyusutan/amortisasi otomatis ikut masuk dari tab aset.</p>{corrections.map((item) => <div className="recon-row" key={item.id}><input value={item.name} onChange={(e) => updateCorrection(item.id, 'name', e.target.value)} /><select value={item.type} onChange={(e) => updateCorrection(item.id, 'type', e.target.value)}><option value="positive">Koreksi Positif</option><option value="negative">Koreksi Negatif</option></select><input type="number" min="0" value={item.amount} onChange={(e) => updateCorrection(item.id, 'amount', e.target.value)} /><button className="icon-btn" onClick={() => setCorrections((items) => items.filter((x) => x.id !== item.id))}>×</button></div>)}<button className="secondary-btn" onClick={() => setCorrections((items) => [...items, { id: Date.now(), name: 'Koreksi lain', type: 'positive', amount: '0' }])}>+ Tambah koreksi</button><div className="mini-results"><span>Total koreksi positif <b>{formatRupiah(positive)}</b></span><span>Total koreksi negatif <b>{formatRupiah(negative)}</b></span></div><div className="eyebrow" style={{ marginTop: 8 }}>Kompensasi kerugian fiskal</div><p className="corp-help">Isi saldo rugi fiskal yang masih dapat digunakan, maksimal lima tahun secara umum.</p><div className="loss-grid">{losses.map((value, index) => <label key={index}><span>Tahun N-{index + 1}</span><input type="number" min="0" value={value} onChange={(e) => setLosses((items) => items.map((x, i) => i === index ? e.target.value : x))} /></label>)}</div>{scheme === 'final05' && <div className="warning-box">Data rekonsiliasi tetap dapat disimpan untuk analisis, tetapi tidak dipakai menghitung PPh Final UMKM 0,5%.</div>}</>}
    {section === 'assets' && <><div className="eyebrow">Penyusutan & Amortisasi Fiskal</div><p className="corp-help">Perhitungan tahun pertama diprorata dari bulan perolehan. Bangunan menggunakan garis lurus.</p>{assets.map((asset) => { const calc = assetCalc.find((x) => x.id === asset.id), group = depreciationRates[asset.group] || depreciationRates.group1; return <div className="asset-card" key={asset.id}><div className="asset-head"><input value={asset.name} onChange={(e) => updateAsset(asset.id, 'name', e.target.value)} /><button className="icon-btn" onClick={() => setAssets((items) => items.filter((x) => x.id !== asset.id))}>×</button></div><div className="asset-grid"><label><span>Jenis</span><select value={asset.kind} onChange={(e) => updateAsset(asset.id, 'kind', e.target.value)}><option value="tangible">Harta berwujud</option><option value="intangible">Harta tak berwujud</option></select></label><label><span>Nilai perolehan</span><input type="number" min="0" value={asset.cost} onChange={(e) => updateAsset(asset.id, 'cost', e.target.value)} /></label><label><span>Bulan perolehan</span><select value={asset.month} onChange={(e) => updateAsset(asset.id, 'month', e.target.value)}>{Array.from({ length: 12 }, (_, i) => <option key={i + 1} value={i + 1}>{i + 1}</option>)}</select></label><label><span>Kelompok fiskal</span><select value={asset.group} onChange={(e) => updateAsset(asset.id, 'group', e.target.value)}>{Object.entries(depreciationRates).map(([key, item]) => <option key={key} value={key}>{item.label}</option>)}</select></label><label><span>Metode</span><select value={group.db === null ? 'sl' : asset.method} disabled={group.db === null} onChange={(e) => updateAsset(asset.id, 'method', e.target.value)}><option value="sl">Garis lurus</option>{group.db !== null && <option value="db">Saldo menurun</option>}</select></label><label><span>Penyusutan/amortisasi komersial</span><input type="number" min="0" value={asset.commercial} onChange={(e) => updateAsset(asset.id, 'commercial', e.target.value)} /></label></div><div className="asset-result"><span>Fiskal: <b>{formatRupiah(calc.fiscal)}</b></span><span>{calc.positive > 0 ? 'Koreksi +' : calc.negative > 0 ? 'Koreksi -' : 'Tidak ada koreksi'}: <b>{formatRupiah(calc.positive || calc.negative)}</b></span></div></div>; })}<button className="secondary-btn" onClick={() => setAssets((items) => [...items, { id: Date.now(), name: 'Aset baru', kind: 'tangible', cost: '0', month: '1', group: 'group1', method: 'sl', commercial: '0' }])}>+ Tambah aset</button></>}
    {section === 'credits' && <><div className="eyebrow">Kredit Pajak</div>{scheme === 'final05' ? <div className="info-box">Pada skema PPh Final UMKM 0,5%, panel kredit pajak PPh 22/23/24/25 tidak digunakan untuk menghitung PPh Final terutang dalam kalkulator ini.</div> : <><Money label="Kredit PPh Pasal 22" value={credits.p22} setValue={(value) => setCredits({ ...credits, p22: value })} /><Money label="Kredit PPh Pasal 23" value={credits.p23} setValue={(value) => setCredits({ ...credits, p23: value })} /><Money label="Kredit PPh Pasal 24" value={credits.p24} setValue={(value) => setCredits({ ...credits, p24: value })} /><Money label="Angsuran PPh Pasal 25" value={credits.p25} setValue={(value) => setCredits({ ...credits, p25: value })} /><Money label="Kredit pajak lain yang dapat diperhitungkan" value={credits.other} setValue={(value) => setCredits({ ...credits, other: value })} /><div className="info-box">Kredit pajak hanya boleh dimasukkan apabila memenuhi syarat untuk dikreditkan terhadap PPh Badan terutang.</div></>}</>}
  </section><section className="panel result-panel corp-result"><div className="eyebrow">Hasil PPh Badan</div><div className="hero-result"><span>{scheme === 'final05' ? (final05Eligible ? 'PPh Final UMKM terutang' : 'Status skema') : (balance >= 0 ? 'PPh Pasal 29 kurang bayar' : 'PPh lebih bayar')}</span><strong>{scheme === 'final05' && !final05Eligible ? 'Periksa kelayakan' : formatRupiah(Math.abs(scheme === 'final05' ? taxDue : balance))}</strong></div><Row label="Skema tarif" value={calculationLabel} strong /><Row label="Peredaran bruto" value={formatRupiah(rev)} />{scheme !== 'final05' && <><Row label="Laba/(rugi) komersial sebelum pajak" value={formatRupiah(commercialProfit)} /><Row label="Koreksi fiskal positif" value={formatRupiah(positive)} /><Row label="Koreksi fiskal negatif" value={formatRupiah(negative)} /><Row label="Penghasilan neto fiskal" value={formatRupiah(fiscalNet)} strong /><Row label="Kompensasi kerugian digunakan" value={formatRupiah(lossUsed)} note={`Saldo tersedia ${formatRupiah(lossAvailable)}`} /><Row label="Penghasilan Kena Pajak (PKP)" value={formatRupiah(pkp)} strong />{scheme === 'auto' && facilityPkp > 0 && <Row label="PKP fasilitas Pasal 31E" value={formatRupiah(facilityPkp)} note="Tarif efektif 11%" />}<Row label={scheme === 'auto' ? 'PKP tarif umum' : 'Dasar PKP'} value={formatRupiah(normalPkp)} note={scheme === 'listed19' ? 'Tarif 19%' : scheme === 'manual' ? `Tarif ${Number(manualRate) || 0}%` : 'Tarif 22%'} /><Row label="PPh Badan terutang" value={formatRupiah(taxDue)} strong /><Row label="Total kredit pajak" value={formatRupiah(totalCredits)} /></>}{scheme === 'final05' && <><Row label="Jenis WP Badan" value={entityType === 'soleCompany' ? 'Perseroan Perorangan' : entityType === 'cooperative' ? 'Koperasi' : 'Badan lain'} /><Row label="Batas omzet" value="Rp4.800.000.000" /><Row label="Tarif final" value="0,5% dari omzet" strong /><Row label="PPh Final terutang" value={final05Eligible ? formatRupiah(taxDue) : 'Tidak dapat dihitung'} /></>}<div className="formula">{formulaText}</div></section></div></div>;
}

export default function TaxCalculator() {
  const [active, setActive] = useState('pph21'), [dialog, setDialog] = useState(null), card = cards[active];
  const openRegulation = () => { if (card.links.length === 1) window.open(card.links[0][1], '_blank', 'noopener,noreferrer'); else setDialog(card); };
  return <main><header className="topbar"><div className="brand-mark">KP</div><div className="brand-copy"><b>Kalkulator Pajak Kantor Kencana</b><span>Indonesia</span></div><div className="verified-pill">Aturan diverifikasi 21 Agustus 2026</div></header><section className="hero"><div className="hero-copy"><div className="eyebrow">Kalkulator pajak Kantor Kencana</div><h1>Hitung Pajak dengan lebih <em>praktis</em>.</h1><p>PPh 21, PPN, PPh Final UMKM, PPh Unifikasi, dan PPh Badan dalam satu kalkulator.</p></div><button type="button" className="law-card law-card-button" onClick={openRegulation}><span>{card.kicker}</span><strong>{card.title}</strong><p>{card.text}</p><i>Buka peraturan resmi ↗</i></button></section><nav className="tabs">{tabs.map(([id, label]) => <button key={id} className={active === id ? 'active' : ''} onClick={() => setActive(id)}>{label}</button>)}</nav>{active === 'pph21' && <P21 />}{active === 'ppn' && <PPN />}{active === 'umkm' && <UMKM />}{active === 'unifikasi' && <UNI />}{active === 'badan' && <CorpTax />}<footer><p><b>Catatan:</b> Kalkulator ini membantu estimasi dan bukan pengganti bukti potong, Coretax/DJP, SPT Tahunan, atau penelaahan profesional. Pemilihan tarif/skema PPh Badan harus disesuaikan dengan status Wajib Pajak, peredaran bruto, tahun pajak, serta terpenuhinya seluruh persyaratan fasilitas.</p></footer><style>{`.law-card-button{border:0;text-align:left;width:100%;font:inherit;cursor:pointer}.law-card-button i{display:block;margin-top:16px;padding-top:11px;border-top:1px solid rgba(255,255,255,.18);font-size:10px;font-style:normal;font-weight:800}.law-card-button:hover{transform:translateY(-2px);box-shadow:0 28px 58px rgba(15,74,57,.22)}.reg-overlay{position:fixed;inset:0;z-index:9999;display:grid;place-items:center;padding:20px;background:rgba(10,18,15,.62);backdrop-filter:blur(5px)}.reg-dialog{width:min(500px,100%);padding:24px;border-radius:20px;background:#fffdf8;color:#17201c}.reg-dialog h3{font:500 27px Georgia,serif;margin:6px 0 10px}.reg-links{display:grid;gap:9px;margin-top:18px}.reg-links a{display:flex;justify-content:space-between;padding:13px 14px;border:1px solid #d8d2c6;border-radius:12px;background:white;color:#0f4a39;text-decoration:none;font-size:12px;font-weight:800}.reg-close,.secondary-btn{border:0;border-radius:11px;padding:11px 14px;font-weight:800;cursor:pointer}.reg-close{width:100%;margin-top:14px;background:#17201c;color:#fff}.secondary-btn{background:#e8efe9;color:#0f4a39}.corp-tabs{display:flex;gap:6px;overflow-x:auto;margin-bottom:14px;padding:6px;border:1px solid #d8d2c6;border-radius:15px;background:rgba(255,255,255,.45)}.corp-tabs button{border:0;background:transparent;padding:10px 13px;border-radius:9px;font-size:11px;font-weight:800;white-space:nowrap}.corp-tabs button.active{background:#17614a;color:white}.corp-form,.corp-result{min-height:650px}.corp-help{margin:0;color:#68716c;font-size:11px;line-height:1.55}.corp-summary-box{display:flex;flex-direction:column;gap:7px;padding:15px;border-radius:13px;background:#f0ece3;font-size:11px;line-height:1.55}.rate-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.rate-grid div{display:flex;flex-direction:column;gap:4px;padding:12px 8px;border:1px solid #d8d2c6;border-radius:12px;background:#fff;text-align:center}.rate-grid span{font:500 22px Georgia,serif;color:#0f4a39}.rate-grid small{font-size:9px;color:#68716c}.rate-note{margin-top:-10px;color:#68716c;font-size:9px}.recon-row{display:grid;grid-template-columns:1.5fr .8fr .9fr 36px;gap:8px}.recon-row input,.recon-row select{min-height:42px;padding:9px 10px}.icon-btn{border:0;border-radius:9px;background:#f1e6df;color:#7b3629;font-size:20px}.mini-results{display:grid;grid-template-columns:1fr 1fr;gap:8px}.mini-results span{display:flex;flex-direction:column;gap:5px;padding:12px;border-radius:11px;background:#f0ece3;font-size:10px}.loss-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.loss-grid label,.asset-grid label{display:flex;flex-direction:column;gap:6px;font-size:10px;font-weight:700}.loss-grid input,.asset-grid input,.asset-grid select{min-height:42px;padding:9px 10px}.asset-card{padding:14px;border:1px solid #d8d2c6;border-radius:14px;background:#fff}.asset-head{display:grid;grid-template-columns:1fr 36px;gap:8px;margin-bottom:10px}.asset-head input{min-height:42px}.asset-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:9px}.asset-result{display:flex;justify-content:space-between;gap:10px;margin-top:11px;padding-top:10px;border-top:1px solid #e5dfd5;font-size:10px}@media(max-width:700px){.rate-grid{grid-template-columns:repeat(2,1fr)}.recon-row{grid-template-columns:1fr}.recon-row .icon-btn{height:40px}.asset-grid,.loss-grid,.mini-results{grid-template-columns:1fr}.asset-result{flex-direction:column}}`}</style>{dialog && <div className="reg-overlay" onMouseDown={() => setDialog(null)}><section className="reg-dialog" onMouseDown={(e) => e.stopPropagation()}><div className="eyebrow">Sumber resmi</div><h3>{dialog.title}</h3><p className="corp-help">Pilih peraturan atau halaman resmi yang ingin dibuka. Tautan akan terbuka di tab baru.</p><div className="reg-links">{dialog.links.map(([label, url]) => <a key={url} href={url} target="_blank" rel="noopener noreferrer"><span>{label}</span><span>↗</span></a>)}</div><button className="reg-close" onClick={() => setDialog(null)}>Tutup</button></section></div>}</main>;
}
